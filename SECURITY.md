# Chính sách bảo mật và dữ liệu

Không mở issue công khai chứa lỗ hổng chưa vá, PII, dữ liệu pilot hoặc secret.
Hãy báo riêng cho chủ dự án/giảng viên qua kênh nội bộ đã thống nhất, kèm phiên
bản, cách tái hiện tối thiểu và tác động; không đính kèm dữ liệu người tham gia.

FocusMate lưu dữ liệu mặc định trong trình duyệt. Người đóng góp phải:

- Không thêm telemetry/cloud sync nếu chưa có consent, threat model và phê duyệt.
- Không ghi token, email, họ tên, mã sinh viên hoặc bảng nối danh tính vào repo.
- Kiểm tra file xuất/nhập không vượt khỏi schema công khai.
- Cập nhật dependency/notice và kiểm tra nguồn trước khi phát hành artefact.

Chỉ phiên bản release mới nhất sau này mới được hỗ trợ bảo mật. Trong giai đoạn
MVP, mọi bản build là thử nghiệm và không dùng cho quyết định sức khỏe.
